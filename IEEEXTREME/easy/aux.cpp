#include <algorithm>
#include <iostream>
#include <numeric>
#include <vector>

using namespace std;
// Custom comparison function for sorting in descending order
bool descendingOrder(int a, int b) { return a > b; }

int main()
{
  //
  // a = 130660965862333;
  // cout << sizeof(unsigned) << endl;
  // cout << sizeof(long long unsigned) << endl;
  // cout << (1 << (sizeof(long long int) * 4 - 1)) << endl;
  // ============================
  // cout << numeric_limits<long long int>::min() << endl;
  // cout << numeric_limits<long long int>::max() << endl;
  // cout << endl;
  // cout << numeric_limits<long long unsigned>::min() << endl;
  // cout << numeric_limits<long long unsigned>::max() << endl;
  // ============================
  cout << sizeof(4) * 8 << endl;
}
