#include <bits/stdc++.h>//solo funciona en g++
//
#include <algorithm>
#include <sstream>// para stringstream
#include <string>
#include <vector>

// long double es para aún más tamaño con decimales
// double es para más tamaño con decimales
// float es para decimales
// -(2^63) <= long long <= 2^63
// -(2^31) <= int <= 2^31
// #define int long long //para convertir todos los int en long long
#define todo(x) (x).begin(), (x).end()

using namespace std;
using strs = stringstream;
using vi = vector<int>;
using ii = pair<int, int>;
using vii = vector<ii>;

void solve()
{
  int n;
  cin >> n;

  for (int i = 0; i < n; i++) {
    int e;
    cin >> e;
  }
}

signed main()
{
  int t;
  cin >> t;

  while (t--) { solve(); }
  // solve();

  return 0;
}
