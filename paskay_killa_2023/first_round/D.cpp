#include <bits/stdc++.h>
#define fastio()                         \
  std::ios_base::sync_with_stdio(false); \
  std::cin.tie(NULL);                    \
  std::cout.tie(NULL)

using lli = long long unsigned;
using u = unsigned;
using llu = long long unsigned;
using ull = unsigned long long;

#define FOR_N(it, limit) for (int it = 0; it < (limit); it++)

using namespace std;

const ull max = 10000;
ull T[max];

int b(int n, )
{
  if (n == 0) {
    //
    return m;
  }
  return m + b(1) + b(2) + b(3) + b(4);
}

void solve()
{
  unsigned n;
  cin >> n;

  FOR_N(i, n)
  {
    int e;
    cin >> e;
  }
}

signed main()
{
  fastio();

  unsigned t;
  cin >> t;

  while (t--) { solve(); }

  return 0;
}
